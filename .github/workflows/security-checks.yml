name: security-checks

on: [push, pull_request]

jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan secrets (fichiers sensibles suivis)
        run: python tools/check_secrets.py
